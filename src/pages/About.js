import React from 'react';
import Header from '../components/Header';

function About() {
    return (
        <div>
            <Header />
            <h2>About</h2>
            <p>This is an example React application with routing and error handling.</p>
        </div>
    );
}

export default About;
